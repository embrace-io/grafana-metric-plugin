# Embrace Metric Grafana Plugin

## Submission Process 
Run `zsh ./make_release.sh 1.2.3` to create the artifacts needed for the review process with Grafana. It will output
the plugin URL, md5 hash, and a link to source code.
